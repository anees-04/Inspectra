# Inspectra
Inspectra is a Manifest V3 browser extension for real-time security auditing. It checks security headers, cookie attributes, CSP configurations, CSRF vulnerabilities, XSS sinks, and sensitive data leakage, enriched with an offline database of 1,000 misconfiguration signatures.
